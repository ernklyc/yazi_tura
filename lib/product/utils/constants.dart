import 'package:flutter/cupertino.dart';

class ProjectPadding {
  final EdgeInsets headsTailseAssetsSymmetric2X =
      const EdgeInsets.symmetric(horizontal: 0, vertical: 0);
  final EdgeInsets headsTailseAssetsSymmetric =
      const EdgeInsets.symmetric(horizontal: 40, vertical: 15);
  final EdgeInsets buttonSymetirc = const EdgeInsets.symmetric(vertical: 80);
  final EdgeInsets gamePageSymetric =
      const EdgeInsets.symmetric(horizontal: 20, vertical: 20);
  final EdgeInsets showDiaologSymetric =
      const EdgeInsets.symmetric(vertical: 140);
}

class ProjectCircular {
  final BorderRadius circular20 = const BorderRadius.all(Radius.circular(20));
  final BorderRadius circular5 = BorderRadius.circular(5);
}

class ProjectNumbers {
  late double headsTailsLetterSpacing = 5;
  late double gamePageHeight = 50;
  late double appBarActionsIcons = 20;
  late double counterSize = 35;
  late double conterTextSize = 25;
}
